package org.campagnelab.circles.model.web;

/*Generated by MPS */

import com.orientechnologies.orient.core.record.impl.ODocument;
import java.util.List;
import com.orientechnologies.orient.core.db.record.OIdentifiable;
import java.util.ArrayList;

public class BaseConcept_DbToPojoHelper {
  public static void transfer(ODocument source, BaseConcept_Bean destination) {
    // transfer properties: 
    destination.shortDescription = source.field("shortDescription");
    destination.alias = source.field("alias");
    destination.virtualPackage = source.field("virtualPackage");
    // transfer references: 
    // transfer children: 
    {
      List<OIdentifiable> childrenValues = source.field("smodelAttribute");
      if (childrenValues != null) {
        destination.smodelAttribute = new ArrayList<Attribute_Bean>();
        for (OIdentifiable childDoc : childrenValues) {
          if (childDoc != null) {
            //  we use reflection here because we need to convert the db class to  
            //  a concrete POJO class to correctly transfer its data: 
            destination.smodelAttribute.add((Attribute_Bean) ReflectionHelper.transformToPojo((ODocument) childDoc));
          }
        }
      }
    }
  }

  public static BaseConcept_Bean transform(ODocument doc) {
    if (doc == null) {
      return null;
    }
    BaseConcept_Bean bean = new BaseConcept_Bean();
    BaseConcept_DbToPojoHelper.transfer(doc, bean);
    return bean;
  }
}
