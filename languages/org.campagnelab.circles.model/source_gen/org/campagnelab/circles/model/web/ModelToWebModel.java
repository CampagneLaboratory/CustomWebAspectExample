package org.campagnelab.circles.model.web;

/*Generated by MPS */

import java.util.logging.Logger;

public class ModelToWebModel {
  private static Logger logger = Logger.getLogger("ModelToWebModel");

  public static Model_WebModel create(Model_Bean bean) {
    Model_WebModel model = (Model_WebModel) WebModelFactory.createWebModel(bean);
    return model;
  }
  public static void transfer(Model_Bean source, Model_WebModel destination) {
    if (source == null) {
      return;
    }
    RootToWebModel.transfer(source, destination);
    destination.name.set(source.name);
    // transfer children: 
    if (source.nodes != null) {
      for (BaseConcept_Bean childBean : source.nodes) {
        if (childBean != null) {
          BaseConcept_WebModel destChild = BaseConceptToWebModel.create(childBean);
          BaseConceptToWebModel.transfer(childBean, destChild);
          destination.nodes.add(destChild);
        }
      }
    }

    // transfer references: 

  }

  public static Model_WebModel transform(Model_Bean bean, ClientFactoryInterface clientFactory) {
    if (bean == null) {
      return null;
    }
    Model_WebModel model = create(bean);
    if (model != null) {
      model.setClientFactory(clientFactory);
    }
    transfer(bean, model);
    return model;
  }
}
