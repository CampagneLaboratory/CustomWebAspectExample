package org.campagnelab.circles.mockup.web;

/*Generated by MPS */

import java.util.logging.Logger;

public class DashboardToWebModel {
  private static Logger logger = Logger.getLogger("DashboardToWebModel");

  public static Dashboard_WebModel create(Dashboard_Bean bean) {
    Dashboard_WebModel model = (Dashboard_WebModel) WebModelFactory.createWebModel(bean);
    return model;
  }
  public static void transfer(Dashboard_Bean source, Dashboard_WebModel destination) {
    if (source == null) {
      return;
    }
    BaseConceptToWebModel.transfer(source, destination);
    
    // ignore interface ;
    destination.query.set(source.query);
    destination.queryTerm.set(source.queryTerm);
    // transfer children: 
    if (source.containedCircles != null) {
      for (CircleRef_Bean childBean : source.containedCircles) {
        if (childBean != null) {
          CircleRef_WebModel destChild = CircleRefToWebModel.create(childBean);
          CircleRefToWebModel.transfer(childBean, destChild);
          destination.containedCircles.add(destChild);
        }
      }
    }

    // transfer references: 

  }

  public static Dashboard_WebModel transform(Dashboard_Bean bean, ClientFactoryInterface clientFactory) {
    if (bean == null) {
      return null;
    }
    Dashboard_WebModel model = create(bean);
    if (model != null) {
      model.setClientFactory(clientFactory);
    }
    transfer(bean, model);
    return model;
  }
}
