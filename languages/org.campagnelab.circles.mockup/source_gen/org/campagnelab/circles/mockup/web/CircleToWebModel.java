package org.campagnelab.circles.mockup.web;

/*Generated by MPS */

import java.util.logging.Logger;

public class CircleToWebModel {
  private static Logger logger = Logger.getLogger("CircleToWebModel");

  public static Circle_WebModel create(Circle_Bean bean) {
    Circle_WebModel model = (Circle_WebModel) WebModelFactory.createWebModel(bean);
    return model;
  }
  public static void transfer(Circle_Bean source, Circle_WebModel destination) {
    if (source == null) {
      return;
    }
    BaseConceptToWebModel.transfer(source, destination);
    
    // ignore interface ;
    
    // ignore interface ;
    destination.query.set(source.query);
    destination.name.set(source.name);
    destination.id.set(source.id);
    // transfer children: 
    if (source.containedCircles != null) {
      for (CircleRef_Bean childBean : source.containedCircles) {
        if (childBean != null) {
          CircleRef_WebModel destChild = CircleRefToWebModel.create(childBean);
          CircleRefToWebModel.transfer(childBean, destChild);
          destination.containedCircles.add(destChild);
        }
      }
    }

    if (source.items != null) {
      for (CircleItem_Bean childBean : source.items) {
        if (childBean != null) {
          CircleItem_WebModel destChild = CircleItemToWebModel.create(childBean);
          CircleItemToWebModel.transfer(childBean, destChild);
          destination.items.add(destChild);
        }
      }
    }

    // transfer references: 

  }

  public static Circle_WebModel transform(Circle_Bean bean, ClientFactoryInterface clientFactory) {
    if (bean == null) {
      return null;
    }
    Circle_WebModel model = create(bean);
    if (model != null) {
      model.setClientFactory(clientFactory);
    }
    transfer(bean, model);
    return model;
  }
}
